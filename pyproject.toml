[project]
name = "graph-theory"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.3",
    "networkx>=3.6.1",
]

[project.scripts]
extract-routes = "graph_theory.extract_routes:main"
